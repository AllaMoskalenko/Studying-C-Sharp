Написать класс "Matrix_3x3" - матрицы размером 3 на 3, содержащий поле:
[,]arr - 2Д массив с целыми элементами;
обычный метод (обращение через ОБЪЕКТ КЛАССА):
Determinant() - возвращает определитель матрицы
статический метод(т.е. такой, к которому надо обращаться через НАЗВАНИЕ КЛАССА):
Add(Matrix_3x3 m1, Matrix_3x3 m2) - на входе которой идет две матрицы, а на выход - одна: их поэлементная сумма.

Существует файл, в котором занесены матрицы (каким образом они там занесены - прояви фантазию🙃)

Задача:
1. Считать все матрицы из файла;
2. Дать возможность пользователю с помощью двух ComboBox-ов выбрать две любых (не обязательно разных матрицы) и показать их на экран;
3. Посчитать сумму этих двух матриц и, наравне с другими двумя, показать на экране;
4. Посчитать определитель всех трех матриц
